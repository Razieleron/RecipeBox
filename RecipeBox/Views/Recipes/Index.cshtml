@{
  Layout = "_Layout";
}

@using RecipeBox.Models;

<h2> Recipe List </h2>

@if(Model.Count == 0)
{
  <h3>No Recipes Yet!</h3>
}

@foreach (Recipe item in Model)
{
  <p>@Html.ActionLink($"{item.RecipeName}", "Details", new {id = item.RecipeId})</p>
}